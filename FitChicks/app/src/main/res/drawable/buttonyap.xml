<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Arka plan (beyaz) -->
    <solid android:color="#FFFFFF" />

    <!-- Çerçeve (bordo) -->
    <stroke
        android:width="3dp"
        android:color="#5E0B0B" />  <!-- koyu bordo -->

    <!-- Köşe yuvarlaklığı -->
    <corners android:radius="9dp" />

    <!-- İç boşluk -->
    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp" />
</shape>